# SequelNet Schema Generator CLI

Reads a SequelNet macro from standard input and writes a JSON response containing generated code, warnings, and errors. See the repository [macro reference](../Macro%20Structure.MD) for the input syntax.

Install it from a package source:

```powershell
dotnet tool install --global SequelNet.SchemaGenerator.Cli
```